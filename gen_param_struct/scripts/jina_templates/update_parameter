if (param.get_name() == "{{parameter_name}}") {
    result.successful = false;
    {{declare_param_validations}}
    params_.{{parameter_name}} = param.as_double_array();
    result.successful = true;
}
